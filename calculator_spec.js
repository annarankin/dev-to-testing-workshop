const assert = require('assert')

assert.equal(true, true)
// assert.equal(true, false)

function add(num1, num2) {
  num1 + num2
}

// How can we change the code above to make this spec pass?
// assert(add(1, 1), 2)

